{
  "name": "parklife-mockup",
  "version": "1.0.0",
  "private": true,
  "description": "Renders the ParkLife design mockups in docs/screenshots from the shipped content catalog.",
  "type": "module",
  "scripts": {
    "build": "python3 build.py",
    "shoot": "python3 build.py && node shoot.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
